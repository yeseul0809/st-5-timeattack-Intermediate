import TodoItem from "./TodoItem";

export default function TodoList({ isDone }) {
  return (
    <section>
      <h2>Working...</h2>
      <ul>
        <TodoItem />
      </ul>
    </section>
  );
}
